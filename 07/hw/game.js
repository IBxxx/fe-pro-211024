function starGame(){
    let option = ['камень', 'ножницы', 'бумага'];
    let cjmputerChoise = options[math.floor(math.radom()*3)];
    let userchoise = promt('напишите свой выбор камень, ножницы, бумага').trim().toLowerCase();
    if(!options.includes(userChoise)){
        alert()
    }
}